name: remote_config_generator
description: "a generator for remote config"
version: 1.0.0
repository: https://github.com/icapps/flutter_remote_config_generator

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  code_builder: ">=4.11.0 <5.0.0"
  dart_style: ">=3.0.0 <4.0.0"
  flutter:
    sdk: flutter
  googleapis_auth: ">=2.0.0 <3.0.0"
  http: ">=1.3.0 <2.0.0"
  path: ">=1.9.0 <2.0.0"
  yaml: ">=3.1.3 <4.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ">=6.0.0 <7.0.0"

flutter: